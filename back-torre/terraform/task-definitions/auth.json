[
  {
    "name": "${var.project-name}-container-auth",
    "image": "${var.repository_url}",
    "memoryReservation": 128,
    "essential": true,
    "portMappings": [
      {
        "containerPort": 8082,
        "hostPort": 8082
      }
    ]
  }
]
